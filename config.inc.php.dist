<?php

// Force password change Plugin options
// -----------------------
// control enable the plugin or not
// Default is false
$config['force_password_change'] = false;

// Default password change interval (in days).
// value must be an integer number.
// Default is 90 (90 days)
$config['force_password_change_interval'] = 90;
